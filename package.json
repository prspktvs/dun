{
  "name": "dun",
  "version": "1.0.0",
  "description": "Get stuff dun",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd app && npm i && npm run build && cd .."
  },
  "author": "hi@p11.co",
  "license": "ISC",
  "dependencies": {
    "firebase-tools": "^12.6.2"
  }
}
